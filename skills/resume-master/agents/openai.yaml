interface:
  display_name: "简历工作台"
  short_description: "直接编写或按JD定制可编辑HTML简历，并导出可打印PDF文件"
  default_prompt: "Use $resume-master to create or tailor my resume as editable HTML and export a printable PDF."

policy:
  allow_implicit_invocation: true
